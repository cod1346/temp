#class 파일은 제외
*.class

#확장자가 .a 인거 는 무시하지 않음
*.a

#윗 라인에서 확장자가 a인 파일은 무시했지만 lib.a 는 무시하지않음
!lib.a

#현재 디렉토리에 있는 TODO파일은 무시하고 subdir/TODO 처럼 하위 디렉토리에 있는 파일은 무시하지 않음
/TODO

#build 디렉토리에 있는 모든 파일은 무시
build/

#doc 폴더 바로 아래에 txt 파일 무시(doc/server/test.txt 파일은 무시하지 않음(한단계 더 아래의 폴더는 무시x)) (doc/의 txt만 무시)
doc/*.txt

#doc폴더 아래의 모든 .pdf파일 무시(doc/server/1.pdf 무시)
doc/**/*.pdf

Hello.java

Hello.txt